# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "expense-tracker"

[setup]
script = '''
set -euo pipefail

node_major="$(node --version | cut -d. -f1 | tr -d v)"
if [ "$node_major" != "24" ]; then
  echo "SaveYoRupee requires Node 24.x; found $(node --version)"
  exit 1
fi

npm ci
npm run db:generate'''
